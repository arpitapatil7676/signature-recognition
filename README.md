# signature-recognition
"Signature recognition system using a Convolutional Neural Network (CNN) built with TensorFlow/Keras. Trained on the CEDAR signature dataset to classify and verify handwritten signatures."
